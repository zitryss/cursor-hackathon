# Kept in sync with the `workspaces` field in package.json, which bun reads.
packages:
  - "."
  - "packages/*"
  - "extension"

engineStrict: true
nodeVersion: 24.0.0
strictPeerDependencies: true

allowBuilds:
  'esbuild@0.25.12 || 0.28.1': true
