/**
 * TaxLens core — the single estimate engine behind every Taxfix touchpoint.
 *
 * The browser extension detects the opportunity, the Taxfix app manages the
 * decision and the documents, and the tax return completes the value. All
 * three read their numbers from here, so a figure shown on a shop page is the
 * same figure the app files later.
 */

export * from "./estimate";
export * from "./plan";
